<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="SingletonNotepad.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SingletonNotepad.Views"
    xmlns:viewmodels="using:SingletonNotepad.ViewModels"
    x:DataType="local:MainView">

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- MenuBar -->
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="Édition">
                <MenuFlyoutItem Text="Rechercher">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="F"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Remplacer">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="H"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>
            <MenuBarItem Title="Affichage">
                <MenuFlyoutItem Text="Zoom"/>
                <MenuFlyoutItem Text="Plein écran"/>
            </MenuBarItem>
            <MenuBarItem Title="Paramètres">
                <MenuFlyoutItem Text="Paramètres" Click="OnSettingsClick">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="OEM_COMMA"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>
        </MenuBar>

        <!-- CommandBar + Editor -->
        <Grid Grid.Row="1" RowDefinitions="Auto,*">
            <CommandBar Grid.Row="0" IsOpen="False">
                <AppBarButton Icon="Open" Label="Ouvrir" Command="{Binding ViewModel.LoadFileCommand}"/>
                <AppBarButton Icon="Save" Label="Enregistrer" Command="{Binding ViewModel.SaveFileCommand}"/>
                <AppBarSeparator/>
                <AppBarButton Icon="Refresh" Label="Normaliser" Command="{Binding ViewModel.NormalizeCommand}"/>
                <AppBarButton Icon="Copy" Label="Copier"/>
            </CommandBar>

            <TextBox
                Grid.Row="1"
                Text="{Binding ViewModel.EditorContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                FontFamily="Consolas"
                FontSize="14"
                Padding="16"/>
        </Grid>

        <!-- Status bar (WinUI 3 — no native StatusBar; use styled Grid) -->
        <Border Grid.Row="2" BorderThickness="0,1,0,0" Padding="8,4">
            <Grid ColumnDefinitions="*,Auto,Auto">
                <TextBlock Grid.Column="0"
                           Text="{Binding ViewModel.StatusMessage, Mode=OneWay}"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0">
                    <FontIcon Glyph="&#xE74E;" Margin="0,0,6,0" FontSize="12"/>
                    <TextBlock Text="UTF-8" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <FontIcon Glyph="&#xE74F;" Margin="0,0,6,0" FontSize="12"/>
                    <TextBlock x:Name="LineColText" Text="Ln 1, Col 1" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
